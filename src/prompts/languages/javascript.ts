export const JAVASCRIPT_SPECIFIC = `
JavaScript-specific checks:
- Prefer === over == for comparisons
- Check for unhandled promise rejections
- Look for memory leaks (event listeners, timers not cleaned up)
- Verify proper error handling in async functions
- Check for this binding issues in callbacks
- Look for missing await on promises
- Verify proper cleanup in useEffect/lifecycle methods
`;
